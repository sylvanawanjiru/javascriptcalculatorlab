// calculator.js
// 1) Create an empty array to store calculation history 
const calculationHistory = [];

// 2) Function to add a calculation in history
function saveToHistory(number1, number2 operaator, result) {
    number1: number1,
    number2: number2,
    operator: operator,
    result: result,

    // 3) store the date and time 
    //4) add the calculation to the history array

};


